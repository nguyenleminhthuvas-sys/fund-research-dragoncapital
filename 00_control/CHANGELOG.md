# CHANGELOG

| Ngày | Version | Ai | Thay đổi |
|---|---|---|---|
| 2026-08-14 | v1.0 | A0 | T0.1 hoàn thành: khởi tạo cây thư mục, tách GLOBAL_RULES / SCHEMAS / AGENT_ROSTER, tạo 5 file CSV/JSON với header, tạo TASK_BOARD |
| 2026-08-14 | v1.0 | A0 | Tạo `SKILL_INTEGRATION_MAP.md`: bảng skill×phase, 6 xung đột phát hiện, 10 override rules, bảng ánh xạ schema Skill 1 → usecase_registry.csv |
| 2026-08-14 | v1.0 | A0 | Cập nhật `GLOBAL_RULES.md`: thêm mục OVERRIDE SKILL (OS-1 đến OS-10) |
| 2026-08-14 | v1.0 | A0 | Ghi OQ-001 (BLOCKER: Skill 3 rỗng) và OQ-002 (HIGH: thiếu task fact-check 100 UC) vào OPEN_QUESTIONS.md |
| 2026-08-14 | v1.1 | A1 | **T1.1 DONE:** Tạo GLOSSARY.md (210 thuật ngữ, 8 nhóm), CORE_30.md (30 cốt lõi + 15 cơ chế hoạt động + bài test NAV T+1 150 từ). SOURCE_REGISTRY.csv bổ sung 12 nguồn đã xác minh. TASK_BOARD T1.1 → DONE, bổ sung T5.3/T5.4 chờ phê duyệt. |
| 2026-08-16 | v1.2 | — | Xoá UC-030→033 và bản cũ của T-BO-058→067 khỏi registry vì hỏng schema (không khớp cấu trúc cột chuẩn của `usecase_registry.csv`/`task_registry.csv`). `09_roles_tasks/BLOCK_04_RISK_SUMMARY.md` được viết lại để khớp dữ liệu hiện hành (33 tác vụ T-BO-054→087). *Mục này được bổ sung ngược ngày 17/08/2026 vì bản CHANGELOG gốc dừng ở 14/08 dù đã bị tham chiếu tới — xem audit cùng ngày bên dưới.* |
| 2026-08-16/17 | v2.0 | — | Hoàn thành T5.1→T6.2 trên thực tế (dù `TASK_BOARD.md` tại thời điểm 17/08 vẫn ghi TODO — chưa được đồng bộ): sinh, lọc và chấm điểm 43 use case (không phải 100 UC như mục tiêu gốc ở OQ-002 — quyết định thu hẹp phạm vi chưa có ghi chép chính thức, xem `00_control/ASSUMPTIONS.md` AS-004), dựng `report_template.html` + `build_report.py`, xuất `FINAL_REPORT.html`/`index.html` bản v2.0. **T7.1 (Red Team audit) chưa từng chạy lại trên bộ 43 UC/87 task cuối cùng** — `AUDIT_LOG.md` chỉ có audit trên artifact nháp cũ trong `_quarantine/`. |
| 2026-08-17 | v2.1 | Claude (audit) | **Audit + sửa lỗi FINAL_REPORT.html** (10 lượt kiểm tra theo yêu cầu người dùng — xem `00_control/AUDIT_LOG.md`): (1) `build_report.py` — `blocks_todo` tính động thay vì hard-code (sửa lỗi "Performance & Risk Management" hiện cùng lúc ở cả "đã nghiên cứu" và "chưa nghiên cứu"); build ghi đồng thời `index.html` và `FINAL_REPORT.html` từ cùng một nguồn để hai file không còn lệch ngày. (2) Thêm banner "Giai đoạn: Catalogue nghiên cứu" ở đầu trang. (3) Phần 08 (Phân tích sâu) nay đọc `12_deep_dives/*.md` thật: link tới hồ sơ khi có, cảnh báo khi thiếu; viết bổ sung `UC-027.md`, `UC-028.md`, `UC-043.md` (3 use case đứng đầu bảng điểm hiện hành nhưng trước đó không có deep-dive). (4) Đăng ký nguồn `S-VSD-002` (Quyết định 34/QĐ-HĐTV) còn thiếu cho T-BO-041/042/043. (5) Thêm bộ lọc "cần phỏng vấn" ở bảng tác vụ Phần 03. (6) Làm mờ/gạch ngang các nguồn "KHÔNG DÙNG ĐƯỢC" trong bảng nguồn Phần 09. (7) Dọn artifact dấu `\|` mồ côi trong `task_registry.csv` (T-BO-055/056). (8) Lập `ASSUMPTIONS.md` (trước đó gần như trống). |
| 2026-08-17 | v2.2 | Claude (audit, đợt 2) | **Tiếp tục sửa lỗi còn lại:** (1) `next_step` trong `build_report.py` trước đây overclaim "phỏng vấn sẽ nâng phần lớn 43 UC lên VERIFIED" — thực tế `09_roles_tasks/INTERVIEW_SHORTLIST.md` chỉ ánh xạ được 4/43 UC (UC-001/002/003/016, đều khối NAV). Đã sửa để tính đúng số UC thực sự được phủ và nêu rõ cần thêm shortlist cho Settlement/TA/Risk. (2) **Chạy T7.1 — Red Team 10 hạng mục lần đầu tiên trên dữ liệu cuối** (43 UC/87 task, trước đây chỉ chạy trên artifact nháp `_quarantine/`): phát hiện 11 dòng `task_registry.csv` cột `chi_tiet_ref` trích quá 15 từ (đã cắt theo GLOBAL_RULES.md mục 5) và quy tắc xếp rổ `ro` (Quick win/Chiến lược/Nghiên cứu) trong `IMPLEMENTATION_PLAN.md` T6.1 không khớp dữ liệu thực tế (17/43 sai lệch nếu áp lại) — đã cập nhật tài liệu ghi đúng quy tắc ngưỡng `tong_diem` thực sự dùng (≥3,8/≥3,0), không sửa dữ liệu vì tự nhất quán 100%. Công thức `tong_diem` (trọng số 0,35/0,25/0,25/0,15) và `roadmap.json` (43/43 UC đúng 1 đợt, 11 phụ thuộc không có chu trình) xác nhận ĐÚNG, không có lỗi. Chi tiết đầy đủ: `00_control/AUDIT_LOG.md` "AUDIT PHASE 3". T7.1 → DONE. |
| 2026-08-17 | v2.2 | Claude (UX + i18n) | Nâng cấp UX báo cáo: sơ đồ mũi tên cho 11 quan hệ phụ thuộc UC (Phần 06), luồng quy trình dạng chuỗi ô cho 87 tác vụ theo khối (Phần 03, gộp đúng bản chất — không vẽ giả chuỗi nối tiếp cho nhóm "Quản trị rủi ro" 27 tác vụ vốn là chủ đề chứ không phải bước tuần tự), nút về đầu trang, bảng cuộn ngang trên mobile, link chia sẻ trực tiếp 1 use case (#UC-xxx), dịch nghĩa tiếng Việt cho 8 tên khối chức năng và 3 nhãn REG/FACT/INFERENCE trên toàn trang. Deploy thủ công qua `vercel --prod` (project không có Git auto-deploy). |
| 2026-08-17 | — | Claude (plan) | Lập `00_control/PHASE2_PLAN.md` — kế hoạch chi tiết nghiên cứu 4 khối chưa khảo sát (Corporate Actions, Client Reporting, Compliance & Regulatory Reporting, Data Management). Ghi rõ 2 cảnh báo: (a) nguồn luật trong `03_industry_vn/legal/*.md` chưa verify, cấm trích; (b) khung chủ đề trong `_quarantine/VALUE_CHAIN.md` chỉ dùng làm checklist tên gọi, cấm dùng nội dung. Định nghĩa ranh giới với 87 task hiện có (AS-005) để tránh trùng lặp. Mở OQ-003 chờ Thư xác nhận thứ tự ưu tiên trước khi chạy T8.1.
